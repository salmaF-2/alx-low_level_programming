#include "main.h"
/**
 * main - prints its name, followed by a new line.
 * @argc: the number of arguments passed
 * @argv: array represents the actual arguments passed to the program
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
(void)argc;
return (0);
}
